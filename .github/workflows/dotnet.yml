name: Build Quaver Mod

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: windows-latest

    steps:
    - uses: actions/checkout@v3
      with:
        submodules: false
        lfs: true
        ref: ${{ github.ref }}

    - name: Initialize Submodules
      run: |
        git submodule init
        git config submodule.Quaver.Server.Client.update none
        git submodule update --recursive || echo "Submodule update failed, continuing..."

    - name: Force Write Fixed Files
      shell: pwsh
      run: |
        # ========================================================
        # 1. MapManager.cs (From your provided clean source code)
        # ========================================================
        $path1 = "Quaver.Shared/Database/Maps/MapManager.cs"
        Write-Host "Force Writing: $path1"
        $mmCode = @'
        using System;
        using System.Collections.Generic;
        using System.IO;
        using System.Linq;
        using System.Threading.Tasks;
        using Emik;
        using Microsoft.Xna.Framework.Graphics;
        using Quaver.API.Enums;
        using Quaver.API.Maps;
        using Quaver.API.Maps.Parsers;
        using Quaver.Server.Client;
        using Quaver.Server.Client.Objects.Twitch;
        using Quaver.Shared.Assets;
        using Quaver.Shared.Audio;
        using Quaver.Shared.Config;
        using Quaver.Shared.Database.Playlists;
        using Quaver.Shared.Graphics;
        using Quaver.Shared.Graphics.Backgrounds;
        using Quaver.Shared.Graphics.Notifications;
        using Quaver.Shared.Helpers;
        using Quaver.Shared.Modifiers;
        using Quaver.Shared.Online.API.Maps;
        using Quaver.Shared.Screens.Selection.UI.Maps;
        using RestSharp;
        using RestSharp.Extensions;
        using Wobble.Audio.Tracks;
        using Wobble.Bindables;
        using Wobble.Graphics.UI.Dialogs;
        using Wobble.Logging;

        namespace Quaver.Shared.Database.Maps
        {
            public static class MapManager
            {
                public static Bindable<Map> Selected { get; set; } = new Bindable<Map>(null);
                public static List<Mapset> Mapsets { get; set; } = new List<Mapset>();
                public static List<Map> RecentlyPlayed { get; set; } = new List<Map>();
                public static string OsuSongsFolder { get; set; }
                public static string CurrentAudioPath => GetAudioPath(Selected?.Value);
                public static Texture2D CurrentBackground { get; set; }
                public static string CurrentBackgroundPath => GetBackgroundPath(Selected.Value);

                public static event EventHandler<MapsetDeletedEventArgs> MapsetDeleted;
                public static event EventHandler<MapDeletedEventArgs> MapDeleted;
                public static event EventHandler<MapUpdatedEventArgs> MapUpdated;
                public static event EventHandler<SongRequestPlayedEventArgs> SongRequestPlayed;

                public static void SelectMapFromMapset(Mapset mapset)
                {
                    Map Select(List<Map> maps, ModIdentifier mods)
                    {
                        if (maps.Count == 0) return null;
                        var minDelta = Double.PositiveInfinity;
                        Map selection = null;
                        foreach (var map in maps)
                        {
                            var target = ConfigManager.PrioritizedMapDifficulty[map.Mode].Value / 10d;
                            double delta = Math.Abs(map.DifficultyFromMods(mods) - target);
                            if (delta < minDelta)
                            {
                                selection = map;
                                minDelta = delta;
                            }
                        }
                        return selection;
                    }
                    List<Map> prioritized = mapset.Maps.FindAll(x => x.Mode == ConfigManager.PrioritizedGameMode.Value);
                    Selected.Value = Select(prioritized, ModManager.Mods) ?? Select(mapset.Maps, ModManager.Mods);
                }

                public static string GetBackgroundPath(Map map)
                {
                    if (map == null) return "";
                    switch (map.Game)
                    {
                        case MapGame.Osu:
                            var osu = new OsuBeatmap(OsuSongsFolder + map.Directory + "/" + map.Path);
                            return $@"{OsuSongsFolder}/{map.Directory}/{osu.Background}";
                        case MapGame.Quaver:
                            return ConfigManager.SongDirectory + "/" + map.Directory + "/" + map.BackgroundPath;
                        case MapGame.Etterna:
                            return map.BackgroundPath;
                        default:
                            return "";
                    }
                }

                public static string GetBannerPath(Map map)
                {
                    if (map == null) return "";
                    switch (map.Game)
                    {
                        case MapGame.Osu: return "";
                        case MapGame.Quaver: return ConfigManager.SongDirectory + "/" + map.Directory + "/" + map.BannerPath;
                        case MapGame.Etterna: return map.BannerPath;
                        default: return "";
                    }
                }

                public static string GetMapsetBannerPath(Mapset mapset)
                {
                    var map = mapset.Maps.First();
                    switch (map.Game)
                    {
                        case MapGame.Osu: return "";
                        case MapGame.Quaver: return (ConfigManager.SongDirectory + "/" + map.Directory + "/" + map.BannerPath).Replace("\\", "/");
                        case MapGame.Etterna: return map.BannerPath;
                        default: return "";
                    }
                }

                public static string GetAudioPath(Map map)
                {
                    if (map == null) return "";
                    switch (map.Game)
                    {
                        case MapGame.Osu: return OsuSongsFolder + "/" + map.Directory + "/" + map.AudioPath;
                        case MapGame.Quaver: return ConfigManager.SongDirectory + "/" + map.Directory + "/" + map.AudioPath;
                        case MapGame.Etterna: return map.AudioPath;
                        default: return "";
                    }
                }

                public static Map FindMapFromMd5(string md5)
                {
                    foreach (var set in Mapsets)
                    {
                        var found = set.Maps.Find(x => x.Md5Checksum == md5);
                        if (found != null) return found;
                    }
                    return null;
                }

                public static Map FindMapFromOnlineId(int id)
                {
                    foreach (var set in Mapsets)
                    {
                        var found = set.Maps.Find(x => x.MapId == id);
                        if (found != null) return found;
                    }
                    return null;
                }

                public static string GetCustomAudioSamplePath(Map map, string samplePath)
                {
                    switch (map.Game)
                    {
                        case MapGame.Osu: return OsuSongsFolder + "/" + map.Directory + "/" + samplePath;
                        case MapGame.Quaver: return ConfigManager.SongDirectory + "/" + map.Directory + "/" + samplePath;
                        default: return "";
                    }
                }

                public static void ViewOnlineListing(Map map = null)
                {
                    if (map == null) map = Selected.Value;
                    if (map == null) return;
                    if (map.MapId == -1) { NotificationManager.Show(NotificationLevel.Error, "This map is not submitted online!"); return; }
                    BrowserHelper.OpenURL($"https://quavergame.com/mapsets/map/{map.MapId}");
                }

                public static void Delete(Map map, int index)
                {
                    if (map.Game != MapGame.Quaver) { NotificationManager.Show(NotificationLevel.Error, "You cannot delete a map loaded from another game"); return; }
                    try
                    {
                        var mapsetPath = Path.Combine(ConfigManager.SongDirectory.Value, map.Mapset.Directory);
                        var path = Path.Combine(mapsetPath, map.Path);
                        if (Rubbish.Move(path)) MapDatabaseCache.RemoveMap(map);
                        else
                        {
                            ShowFallbackMapDeletionDialog("map", () =>
                            {
                                try { File.Delete(path); } catch (Exception e) { Logger.Error(e, LogType.Runtime); }
                                if (File.Exists(path)) { NotificationManager.Show(NotificationLevel.Error, "Unable to delete the map. Is the file protected?"); return; }
                                MapDatabaseCache.RemoveMap(map);
                                map.Mapset.Maps.Remove(map);
                                if (map.Mapset.Maps.Count == 0) Mapsets.Remove(map.Mapset);
                                PlaylistManager.RemoveMapFromAllPlaylists(map);
                                MapDeleted?.Invoke(typeof(MapManager), new MapDeletedEventArgs(map, index));
                            });
                            return;
                        }
                    }
                    catch (Exception e) { Logger.Error(e, LogType.Runtime); }
                    map.Mapset.Maps.Remove(map);
                    if (map.Mapset.Maps.Count == 0) Mapsets.Remove(map.Mapset);
                    PlaylistManager.RemoveMapFromAllPlaylists(map);
                    MapDeleted?.Invoke(typeof(MapManager), new MapDeletedEventArgs(map, index));
                }

                public static void Delete(Mapset mapset, int index)
                {
                    if (mapset.Maps.Count == 0) return;
                    if (mapset.Maps.First().Game != MapGame.Quaver) { NotificationManager.Show(NotificationLevel.Error, "You cannot delete a mapset loaded from another game"); return; }
                    if (mapset.Maps.Contains(Selected.Value))
                    {
                        var oldTrack = AudioEngine.Track;
                        AudioEngine.Track = new AudioTrackVirtual(300000);
                        if (oldTrack != null && !oldTrack.IsDisposed) oldTrack.Dispose();
                    }
                    try
                    {
                        var directory = Path.Combine(ConfigManager.SongDirectory.Value, mapset.Directory);
                        if (!Rubbish.Move(directory) && Directory.Exists(directory))
                        {
                            ShowFallbackMapDeletionDialog("mapset", () =>
                            {
                                try { Directory.Delete(directory, true); } catch (Exception e) { Logger.Error(e, LogType.Runtime); }
                                if (Directory.Exists(directory)) { NotificationManager.Show(NotificationLevel.Error, "Unable to delete the mapset. Is the directory protected?"); return; }
                                try { mapset.Maps.ForEach(MapDatabaseCache.RemoveMap); } catch (Exception e) { Logger.Error(e, LogType.Runtime); }
                                Mapsets.Remove(mapset);
                                MapsetDeleted?.Invoke(typeof(MapManager), new MapsetDeletedEventArgs(mapset, index));
                                lock (BackgroundHelper.MapsetBanners)
                                {
                                    if (!BackgroundHelper.MapsetBanners.ContainsKey(mapset.Directory)) return;
                                    var banner = BackgroundHelper.MapsetBanners[mapset.Directory];
                                    if (banner != UserInterface.DefaultBanner) banner.Dispose();
                                    BackgroundHelper.MapsetBanners.Remove(mapset.Directory);
                                }
                            });
                            return;
                        }
                    }
                    catch (Exception e) { Logger.Error(e, LogType.Runtime); }
                    try { mapset.Maps.ForEach(MapDatabaseCache.RemoveMap); } catch (Exception e) { Logger.Error(e, LogType.Runtime); }
                    Mapsets.Remove(mapset);
                    MapsetDeleted?.Invoke(typeof(MapManager), new MapsetDeletedEventArgs(mapset, index));
                    lock (BackgroundHelper.MapsetBanners)
                    {
                        if (!BackgroundHelper.MapsetBanners.ContainsKey(mapset.Directory)) return;
                        var banner = BackgroundHelper.MapsetBanners[mapset.Directory];
                        if (banner != UserInterface.DefaultBanner) banner.Dispose();
                        BackgroundHelper.MapsetBanners.Remove(mapset.Directory);
                    }
                }

                public static void UpdateMapToLatestVersion(Map outdated)
                {
                    try
                    {
                        var lookup = new APIRequestMapInformation(outdated.MapId).ExecuteRequest();
                        if (lookup.Status != 200) throw new Exception($"Map updated failed. APIRequestMapInformation failed with status: {lookup.Status}");
                        foreach (var mapset in Mapsets)
                        {
                            var foundMap = mapset.Maps.Find(x => x.Md5Checksum == lookup.Map.Md5);
                            if (foundMap == null) continue;
                            outdated.Mapset.Maps.Remove(outdated);
                            MapDatabaseCache.RemoveMap(outdated);
                            if (outdated.Mapset.Maps.Count == 0) Mapsets.Remove(outdated.Mapset);
                            if (Selected.Value == outdated) Selected.Value = foundMap;
                            PlaylistManager.UpdateMapInPlaylists(outdated, foundMap);
                            MapUpdated?.Invoke(typeof(MapManager), new MapUpdatedEventArgs(outdated, foundMap));
                            return;
                        }
                        var path = $"{ConfigManager.SongDirectory.Value}/{outdated.Directory}/{outdated.Path}";
                        Logger.Important($"Downloading latest version of map: {outdated.Id}", LogType.Runtime);
                        var client = new RestClient($"{OnlineClient.API_ENDPOINT}");
                        client.DownloadData(new RestRequest($"{OnlineClient.API_ENDPOINT}/d/web/map/{outdated.MapId}", Method.GET)).SaveAs(path);
                        Logger.Important($"Successfully downloaded latest version of map: {outdated.Id}", LogType.Runtime);
                        var newMap = Map.FromQua(Qua.Parse(path), path);
                        newMap.CalculateDifficulties();
                        newMap.Id = outdated.Id;
                        newMap.Mapset = outdated.Mapset;
                        newMap.Directory = outdated.Directory;
                        newMap.Path = outdated.Path;
                        newMap.DateAdded = outdated.DateAdded;
                        newMap.TimesPlayed = outdated.TimesPlayed;
                        newMap.LocalOffset = outdated.LocalOffset;
                        MapDatabaseCache.UpdateMap(outdated);
                        outdated.Mapset.Maps.Remove(outdated);
                        outdated.Mapset.Maps.Add(newMap);
                        outdated.Mapset.Maps = newMap.Mapset.Maps.OrderBy(x => x.DifficultyFromMods(ModManager.Mods)).ToList();
                        if (Selected.Value == outdated) Selected.Value = newMap;
                        PlaylistManager.UpdateMapInPlaylists(outdated, newMap);
                        MapUpdated?.Invoke(typeof(MapManager), new MapUpdatedEventArgs(outdated, newMap));
                    }
                    catch (Exception e)
                    {
                        Logger.Error(e, LogType.Runtime);
                        MapUpdated?.Invoke(typeof(MapManager), new MapUpdatedEventArgs(outdated, outdated));
                    }
                }

                public static void PlaySongRequest(SongRequest request, Map map)
                {
                    SongRequestPlayed?.Invoke(typeof(MapManager), new SongRequestPlayedEventArgs(request, map));
                }

                public static bool DetectNewMapsets() => ReloadNewMapsets();

                public static bool ReloadNewMapsets()
                {
                    var found = false;
                    var directory = ConfigManager.SongDirectory.Value;

                    if (string.IsNullOrEmpty(directory) || !Directory.Exists(directory))
                        return false;

                    var files = Directory.GetFiles(directory, "*.*", SearchOption.TopDirectoryOnly)
                        .Where(x => x.EndsWith(".osz") || x.EndsWith(".qp") || x.EndsWith(".smzip")).ToList();

                    foreach (var file in files)
                    {
                        if (!MapsetImporter.Queue.Contains(file))
                        {
                            MapsetImporter.Queue.Add(file);
                            found = true;
                        }
                    }

                    var directories = Directory.GetDirectories(directory);
                    var loadedMapsets = new HashSet<string>();
                    lock (Mapsets)
                    {
                        foreach (var set in Mapsets)
                            loadedMapsets.Add(set.Directory);
                    }

                    foreach (var dir in directories)
                    {
                        var dirName = new DirectoryInfo(dir).Name;

                        if (loadedMapsets.Contains(dirName))
                            continue;

                        try
                        {
                            var mapset = new Mapset { Directory = dirName, Maps = new List<Map>() };
                            var mapFiles = Directory.GetFiles(dir, "*.qua");

                            foreach (var mapFile in mapFiles)
                            {
                                var map = Map.FromQua(Qua.Parse(mapFile), mapFile);
                                map.Mapset = mapset;
                                map.Directory = dirName;
                                map.Path = Path.GetFileName(mapFile);
                                map.CalculateDifficulties();
                                mapset.Maps.Add(map);
                            }

                            if (mapset.Maps.Count > 0)
                            {
                                mapset.Maps = mapset.Maps.OrderBy(x => x.DifficultyFromMods(ModManager.Mods)).ToList();
                                
                                lock (Mapsets)
                                    Mapsets.Add(mapset);

                                found = true;
                            }
                        }
                        catch (Exception e)
                        {
                            Logger.Error($"Failed to smart-load mapset: {dirName}. Exception: {e}", LogType.Runtime);
                        }
                    }

                    return found;
                }

                private static void ShowFallbackMapDeletionDialog(string label, Action onYes) =>
                    DialogManager.Show(
                        new YesNoDialog(
                            "Map Deletion",
                            $"Failed to move the {label} in the recycle bin.\nWould you like to delete it instead?",
                            onYes
                        )
                    );
            }
        }
        '@
        Set-Content -Path $path1 -Value $mmCode -Encoding UTF8
        if (Test-Path "BuildPatches/MapManager.cs") { Set-Content -Path "BuildPatches/MapManager.cs" -Value $mmCode -Encoding UTF8 }

        # ========================================================
        # 2. MapDatabaseCache.cs (From your provided clean source code)
        # ========================================================
        $path2 = "Quaver.Shared/Database/Maps/MapDatabaseCache.cs"
        Write-Host "Force Writing: $path2"
        $mdcCode = @'
        using System;
        using System.Collections.Generic;
        using System.IO;
        using System.Linq;
        using osu.Shared;
        using osu_database_reader.BinaryFiles;
        using Quaver.API.Enums;
        using Quaver.API.Maps;
        using Quaver.API.Maps.Processors.Difficulty.Rulesets.Keys;
        using Quaver.Shared.Audio;
        using Quaver.Shared.Config;
        using Quaver.Shared.Database.Playlists;
        using Quaver.Shared.Database.Settings;
        using Quaver.Shared.Graphics.Notifications;
        using SharpCompress.Archives;
        using SharpCompress.Common;
        using SQLite;
        using Wobble;
        using Wobble.Logging;
        using GameMode = osu.Shared.GameMode;

        namespace Quaver.Shared.Database.Maps
        {
            public static class MapDatabaseCache
            {
                public static List<Map> MapsToUpdate { get; } = new List<Map>();

                private static List<string> DefaultMapsetFiles { get; } = new List<string>()
                {
                    "+TEK - Stars and Bunnies",
                    "HyuN - CrossOver",
                    "HyuN - Princess Of Winter",
                    "Rabbit House - Have A Party Time!",
                    "zetoban - Csikos Post",
                    "zetoban - Umami Packed Mountaineering",
                    "zetoban - Flowering Night",
                    "Plum - Fantasy Collision",
                    "Lollipop - vibing oustide the supermarket at 1am",
                    "HyuN feat. Yuri - Disorder",
                    "Rabbit House - Dead Rabbit And Witch's Blood"
                };

                private static string DefaultMapChecksum { get; } = "5004c3553cd29ccd3191e5c266f2f282";

                public static void Load(bool fullSync)
                {
                    CreateTable();
                    var quaFiles = Directory.GetFiles(ConfigManager.SongDirectory.Value, "*.qua", SearchOption.AllDirectories).ToList();
                    Logger.Important($"Found {quaFiles.Count} .qua files inside the song directory", LogType.Runtime);

                    if (fullSync)
                    {
                        SyncMissingOrUpdatedFiles(quaFiles);
                        AddNonCachedFiles(quaFiles);
                    }

                    OrderAndSetMapsets();

                    if (MapManager.Mapsets.Count == 0)
                        ImportDefaultMapsets();
                }

                private static void CreateTable()
                {
                    try
                    {
                        DatabaseManager.Connection.CreateTable<Map>();
                        Logger.Important($"Map Database has been created", LogType.Runtime);
                    }
                    catch (Exception e)
                    {
                        Logger.Error(e, LogType.Runtime);
                        throw;
                    }
                }

                private static void SyncMissingOrUpdatedFiles(IReadOnlyCollection<string> files)
                {
                    var maps = FetchAll();
                    var fileHashSet = files.ToHashSet();

                    foreach (var map in maps)
                    {
                        var filePath = BackslashToForward($"{ConfigManager.SongDirectory.Value}/{map.Directory}/{map.Path}");

                        if (fileHashSet.Contains(BackslashToForward(filePath)))
                        {
                            if (map.LastFileWrite != File.GetLastWriteTimeUtc(filePath))
                            {
                                if (map.Md5Checksum == MapsetHelper.GetMd5Checksum(filePath))
                                    continue;

                                Map newMap;
                                try
                                {
                                    newMap = Map.FromQua(map.LoadQua(false), filePath);
                                }
                                catch (Exception e)
                                {
                                    Logger.Error(e, LogType.Runtime);
                                    File.Delete(filePath);
                                    DatabaseManager.Connection.Delete(map);
                                    Logger.Important($"Removed {filePath} from the cache, as the file could not be parsed.", LogType.Runtime);
                                    continue;
                                }

                                newMap.CalculateDifficulties();
                                newMap.Id = map.Id;
                                DatabaseManager.Connection.Update(newMap);
                                Logger.Important($"Updated cached map: {newMap.Id}, as the file was updated.", LogType.Runtime);
                            }
                            continue;
                        }

                        DatabaseManager.Connection.Delete(map);
                        Logger.Important($"Removed {filePath} from the cache, as the file no longer exists", LogType.Runtime);
                    }
                }

                private static void AddNonCachedFiles(List<string> files)
                {
                    var maps = FetchAll();
                    var hashset = new HashSet<string>();
                    maps.ForEach(x => hashset.Add(BackslashToForward($"{ConfigManager.SongDirectory.Value}/{x.Directory}/{x.Path}")));

                    foreach (var file in files)
                    {
                        if (hashset.Contains(BackslashToForward(file)))
                            continue;

                        try
                        {
                            var map = Map.FromQua(Qua.Parse(file, false), file);
                            InsertMap(map);

                            if (!QuaverSettingsDatabaseCache.OutdatedMaps.Contains(map))
                                QuaverSettingsDatabaseCache.OutdatedMaps.Add(map);
                        }
                        catch (Exception e)
                        {
                            Logger.Error(e, LogType.Runtime);
                        }
                    }
                }

                public static List<Map> FetchAll() => DatabaseManager.Connection.Table<Map>().ToList();
                private static string BackslashToForward(string p) => p.Replace("\\", "/");

                public static int InsertMap(Map map)
                {
                    try
                    {
                        DatabaseManager.Connection.Insert(map);
                        return DatabaseManager.Connection.Get<Map>(x => x.Md5Checksum == map.Md5Checksum).Id;
                    }
                    catch (Exception e)
                    {
                        var existing = DatabaseManager.Connection.Find<Map>(x => x.Md5Checksum == map.Md5Checksum);
                        if (existing == null)
                        {
                            Logger.Error(e, LogType.Runtime);
                            return -1;
                        }

                        var newPath = Path.Combine(ConfigManager.SongDirectory.Value, map.Directory, map.Path);
                        var existingPath = Path.Combine(ConfigManager.SongDirectory.Value, existing.Directory, existing.Path);

                        if (existingPath != newPath)
                        {
                            Logger.Warning($"Tried importing a duplicate of `{existingPath}` at `{newPath}`, deleting.", LogType.Runtime);
                            File.Delete(newPath);
                            return -1;
                        }

                        Logger.Warning($"Tried importing `{existingPath}` twice.", LogType.Runtime);
                        return -1;
                    }
                }

                public static void UpdateMap(Map map)
                {
                    try
                    {
                        DatabaseManager.Connection.Update(map);
                        Logger.Debug($"Updated map: {map.Md5Checksum} (#{map.Id}) in the cache", LogType.Runtime);
                    }
                    catch (Exception e)
                    {
                        Logger.Error(e, LogType.Runtime);
                    }
                }

                public static void RemoveMap(Map map)
                {
                    try
                    {
                        DatabaseManager.Connection.Delete(map);
                        Logger.Debug($"Deleted map: {map.Md5Checksum} (#{map.Id}) in the cache", LogType.Runtime);
                    }
                    catch (Exception e)
                    {
                        Logger.Error(e, LogType.Runtime);
                    }
                }

                public static Map FindSet(int id)
                {
                    try
                    {
                        return DatabaseManager.Connection.Find<Map>(x => x.MapSetId == id);
                    }
                    catch (Exception e)
                    {
                        Logger.Error(e, LogType.Runtime);
                        return null;
                    }
                }

                public static void OrderAndSetMapsets(bool skipPlaylistLoad = false)
                {
                    OtherGameMapDatabaseCache.Initialize();
                    var maps = FetchAll();

                    if (ConfigManager.AutoLoadOsuBeatmaps.Value)
                        maps = maps.Concat(OtherGameMapDatabaseCache.Load()).ToList();

                    var mapsets = MapsetHelper.ConvertMapsToMapsets(maps);
                    MapManager.Mapsets = MapsetHelper.OrderMapsByDifficulty(MapsetHelper.OrderMapsetsByArtist(mapsets));
                    MapManager.RecentlyPlayed = new List<Map>();

                    if (!skipPlaylistLoad)
                        PlaylistManager.Load();

                    foreach (var mapset in MapManager.Mapsets)
                    {
                        foreach (var map in mapset.Maps)
                        {
                            if (map.RegularNoteCount + map.LongNoteCount >= 2)
                            {
                                if (map.Difficulty105X == 0f && !OtherGameMapDatabaseCache.MapsToCache[OtherGameCacheAction.Add].Contains(map))
                                    OtherGameMapDatabaseCache.MapsToCache[OtherGameCacheAction.Update].Add(map);
                            }
                        }
                    }

                    var outdated = FetchAll().FindAll(x => x.DifficultyProcessorVersion != DifficultyProcessorKeys.Version);
                    OtherGameMapDatabaseCache.MapsToCache[OtherGameCacheAction.Update].AddRange(outdated);
                }

                public static void ForceUpdateMaps(bool createNewMaps = true)
                {
                    for (var i = 0; i < MapsToUpdate.Count; i++)
                    {
                        try
                        {
                            var path = $"{ConfigManager.SongDirectory}/{MapsToUpdate[i].Directory}/{MapsToUpdate[i].Path}";
                            if (!File.Exists(path))
                                continue;

                            Map map;
                            if (createNewMaps)
                                map = Map.FromQua(Qua.Parse(path, false), path);
                            else
                                map = MapsToUpdate[i];

                            map.CalculateDifficulties();
                            map.Id = MapsToUpdate[i].Id;
                            map.Directory = MapsToUpdate[i].Directory;
                            map.Mapset = MapsToUpdate[i].Mapset;

                            if (map.Id == 0)
                                map.Id = InsertMap(map);
                            else
                            {
                                UpdateMap(map);
                                PlaylistManager.UpdateMapInPlaylists(MapsToUpdate[i], map);
                            }

                            MapsToUpdate[i] = map;

                            if (createNewMaps)
                                MapManager.Selected.Value = map;
                        }
                        catch (Exception e)
                        {
                            Logger.Error(e, LogType.Runtime);
                        }
                    }

                    MapsToUpdate.Clear();

                    if (createNewMaps)
                    {
                        OrderAndSetMapsets();
                        PlaylistManager.Load();

                        var selectedMapset = MapManager.Mapsets.Find(x => x.Maps.Any(y => y.Md5Checksum == MapManager.Selected.Value.Md5Checksum));
                        MapManager.Selected.Value = selectedMapset.Maps.Find(x => x.Md5Checksum == MapManager.Selected.Value.Md5Checksum);
                    }
                }

                private static void ImportDefaultMapsets()
                {
                    foreach (var map in DefaultMapsetFiles)
                    {
                        var directory = $"{ConfigManager.SongDirectory.Value}/{map}";
                        if (Directory.Exists(directory))
                            continue;

                        Directory.CreateDirectory(directory);
                        try
                        {
                            var stream = GameBase.Game.Resources.GetStream($"Quaver.Resources/DefaultMaps/{map}.qp");
                            using (var archive = ArchiveFactory.Open(stream))
                            {
                                foreach (var entry in archive.Entries)
                                {
                                    if (!entry.IsDirectory)
                                        entry.WriteToDirectory(directory, new ExtractionOptions() { ExtractFullPath = true, Overwrite = true });
                                }
                            }
                            Logger.Important($"Successfully imported default map: {map}", LogType.Runtime);
                        }
                        catch (Exception e)
                        {
                            Logger.Error($"Failed to import default map: {map}", LogType.Runtime);
                            Logger.Error(e, LogType.Runtime);
                        }
                    }

                    Load(true);
                    var defaultMap = MapManager.FindMapFromMd5(DefaultMapChecksum);
                    if (defaultMap != null)
                    {
                        MapManager.Selected.Value = defaultMap;
                        AudioEngine.LoadCurrentTrack();
                    }
                }
            }
        }
        '@
        Set-Content -Path $path2 -Value $mdcCode -Encoding UTF8

        # ========================================================
        # 3. ReplayHelper.cs (LLM-Optimized token compression)
        # ========================================================
        $path3 = "Quaver.Shared/Helpers/ReplayHelper.cs"
        Write-Host "Force Writing: $path3"
        $rhCode = @'
        using System;
        using System.IO;
        using System.Text;
        using System.Linq;
        using System.Collections.Generic;
        using System.Globalization;
        using Quaver.API.Enums;
        using Quaver.API.Maps;
        using Quaver.API.Maps.Processors.Scoring;
        using Quaver.API.Maps.Processors.Scoring.Data;
        using Quaver.API.Replays;
        using Quaver.Shared.Config;
        using Wobble.Logging;

        namespace Quaver.Shared.Helpers
        {
            public static class ReplayHelper
            {
                internal static Replay GeneratePerfectReplay(Qua map, string md5)
                {
                    var replay = new Replay(map.Mode, "Autoplay", Quaver.Shared.Modifiers.ModManager.Mods, md5);
                    if (Quaver.API.Helpers.ModeHelper.IsKeyMode(map.Mode)) return Replay.GeneratePerfectReplayKeys(replay, map);
                    throw new ArgumentOutOfRangeException();
                }

                public static void ExportDetailedAnalysis(List<HitStat> stats, Qua map, ScoreProcessor processor = null, string filename = "last_play_analysis.txt")
                {
                    try
                    {
                        var sb = new StringBuilder();
                        var inv = CultureInfo.InvariantCulture;

                        // --- 1. HEADER: Metadata & Scroll Settings ---
                        sb.AppendLine($"Map: {map.Artist} - {map.Title} [{map.DifficultyName}]");
                        sb.AppendLine($"Mode: {map.Mode} | BPM: {map.GetCommonBpm().ToString("F1", inv)} | Notes: {map.HitObjects.Count}");

                        int scrollSpeed = 0;
                        string scrollDir = "Down";
                        try
                        {
                            if (map.Mode == GameMode.Keys4) { scrollSpeed = ConfigManager.ScrollSpeed4K?.Value ?? 0; scrollDir = ConfigManager.ScrollDirection4K?.Value.ToString() ?? "Down"; }
                            else if (map.Mode == GameMode.Keys7) { scrollSpeed = ConfigManager.ScrollSpeed7K?.Value ?? 0; scrollDir = ConfigManager.ScrollDirection7K?.Value.ToString() ?? "Down"; }
                        } catch { }

                        double visMs = scrollSpeed > 0 ? 610000.0 / scrollSpeed : 0;
                        sb.AppendLine($"ScrollSpeed: {scrollSpeed} | ScrollDirection: {scrollDir} | NoteVisibilityWindowMs: {visMs.ToString("F1", inv)}");

                        // --- 2. HEADER: Judgments & Accuracy ---
                        if (processor != null)
                        {
                            try {
                                var w = processor.JudgementWindow;
                                if (w != null) sb.AppendLine($"HitWindowsMs: Marv:{w[Judgement.Marv].ToString("F1", inv)} Perf:{w[Judgement.Perf].ToString("F1", inv)} Great:{w[Judgement.Great].ToString("F1", inv)} Good:{w[Judgement.Good].ToString("F1", inv)} Okay:{w[Judgement.Okay].ToString("F1", inv)} Miss:{w[Judgement.Miss].ToString("F1", inv)}");
                            } catch { }
                            try {
                                var j = processor.CurrentJudgements;
                                sb.AppendLine($"Judgments: Marv:{j[Judgement.Marv]} Perf:{j[Judgement.Perf]} Great:{j[Judgement.Great]} Good:{j[Judgement.Good]} Okay:{j[Judgement.Okay]} Miss:{j[Judgement.Miss]}");
                                sb.AppendLine($"Accuracy: {processor.Accuracy.ToString("F2", inv)}% | Score: {processor.Score} | MaxCombo: {processor.MaxCombo}");
                            } catch { }
                        }

                        var hitDiffs = stats.Where(s => s.Judgement != Judgement.Miss).Select(s => (double)s.HitDifference).ToList();
                        if (hitDiffs.Count > 0)
                        {
                            double mean = hitDiffs.Average();
                            double std = Math.Sqrt(hitDiffs.Select(x => (x - mean) * (x - mean)).Average());
                            sb.AppendLine($"MeanErrorMs: {mean.ToString("F2", inv)} | StdDevMs: {std.ToString("F2", inv)}");
                        }

                        sb.AppendLine($"PatternProfile: {ComputePatternProfile(map)}");
                        sb.AppendLine();

                        // --- 3. SEGMENTS: 10s LLM Context Blocks ---
                        sb.AppendLine("=== SEGMENTS ===");
                        AppendSegments(sb, stats, map, inv);
                        sb.AppendLine();

                        // --- 4. NOTES: Compressed Hit Data ---
                        sb.AppendLine("=== NOTES ===");
                        sb.AppendLine("# FORMAT: Time:LaneType+Diff (N=Normal H=LN-Head T=LN-Tail M=Miss)");

                        var expected = new List<(float Time, int Lane, KeyPressType Type, bool IsLN)>();
                        foreach (var note in map.HitObjects)
                        {
                            expected.Add((note.StartTime, note.Lane, KeyPressType.Press, note.IsLongNote));
                            if (note.IsLongNote) expected.Add((note.EndTime, note.Lane, KeyPressType.Release, true));
                        }
                        
                        expected = expected.OrderBy(x => x.Time).ThenBy(x => x.Lane).ThenBy(x => x.Type == KeyPressType.Press ? 0 : 1).ToList();

                        var notesList = new List<string>();
                        int count = Math.Min(stats.Count, expected.Count);
                        for (int i = 0; i < count; i++)
                        {
                            var exp = expected[i];
                            char typeChar = exp.Type == KeyPressType.Press ? (exp.IsLN ? 'H' : 'N') : 'T';
                            string diffStr = stats[i].Judgement == Judgement.Miss ? "M" : $"{stats[i].HitDifference:+#0;-#0;0}";
                            notesList.Add($"{exp.Time:F0}:{exp.Lane}{typeChar}{diffStr}");
                        }

                        for (int i = 0; i < notesList.Count; i += 15) sb.AppendLine(string.Join(" ", notesList.Skip(i).Take(15)));

                        string path = Path.Combine(AppDomain.CurrentDomain.BaseDirectory, filename);
                        File.WriteAllText(path, sb.ToString());
                        Logger.Log($"[ReplayHelper] Saved LLM-optimized analysis to: {path}", LogLevel.Important, LogType.Runtime);
                    }
                    catch (Exception e) { Logger.Log($"[ReplayHelper] Error saving analysis: {e.Message}", LogLevel.Important, LogType.Runtime); }
                }

                private static void AppendSegments(StringBuilder sb, List<HitStat> stats, Qua map, CultureInfo inv)
                {
                    if (map.HitObjects.Count == 0 || stats.Count == 0) return;
                    const int segMs = 10000;
                    double totalMs = map.HitObjects[map.HitObjects.Count - 1].StartTime;
                    int numSegs = (int)(totalMs / segMs) + 1;

                    var noteTimes = map.HitObjects.OrderBy(n => n.StartTime).Select(n => (float)n.StartTime).ToList();
                    int n = Math.Min(stats.Count, noteTimes.Count);

                    for (int s = 0; s < numSegs; s++)
                    {
                        double lo = s * segMs;
                        double hi = lo + segMs;
                        var bucketDiffs = new List<int>();
                        int bucketNotes = 0, bucketMiss = 0;
                        var bucketObjects = new List<Quaver.API.Maps.Structures.HitObjectInfo>();
                        
                        for (int i = 0; i < n; i++)
                        {
                            if (noteTimes[i] < lo || noteTimes[i] >= hi) continue;
                            bucketNotes++;
                            bucketObjects.Add(map.HitObjects[i]);
                            if (stats[i].Judgement == Judgement.Miss) bucketMiss++;
                            else bucketDiffs.Add(stats[i].HitDifference);
                        }
                        
                        if (bucketNotes == 0) continue;
                        double acc = 100.0 * (bucketNotes - bucketMiss) / bucketNotes;
                        double mean = bucketDiffs.Count > 0 ? bucketDiffs.Average() : 0;
                        double std = bucketDiffs.Count > 0 ? Math.Sqrt(bucketDiffs.Select(x => (x - mean) * (x - mean)).Average()) : 0;
                        
                        sb.AppendLine($"S{s:D2} [{(int)(lo/1000)}-{(int)(hi/1000)}s] Acc:{acc.ToString("F1", inv)} Mean:{mean.ToString("+#0.0;-#0.0;0.0", inv)} Std:{std.ToString("F1", inv)} Notes:{bucketNotes} Pattern:{DominantPatternTag(bucketObjects)}");
                    }
                }

                private static string DominantPatternTag(List<Quaver.API.Maps.Structures.HitObjectInfo> notes)
                {
                    if (notes.Count == 0) return "empty";
                    int singles=0, jumps=0, hands=0, quads=0, jacks=0, bursts=0;
                    
                    foreach (var g in notes.GroupBy(x => x.StartTime)) {
                        var c = g.Count();
                        if (c <= 1) singles++; else if (c == 2) jumps++; else if (c == 3) hands++; else quads++;
                    }

                    foreach (var col in notes.GroupBy(x => x.Lane)) {
                        var arr = col.OrderBy(x => x.StartTime).ToList();
                        int run = 1;
                        for (int i = 1; i < arr.Count; i++) {
                            if (arr[i].StartTime - arr[i-1].StartTime <= 250) run++;
                            else { if (run >= 3) jacks++; run = 1; }
                        }
                        if (run >= 3) jacks++;
                    }

                    var ordered = notes.OrderBy(x => x.StartTime).ToList();
                    int left = 0;
                    for (int right = 0; right < ordered.Count; right++) {
                        while (ordered[right].StartTime - ordered[left].StartTime > 500) left++;
                        if (right - left + 1 >= 10) { bursts++; break; }
                    }

                    var parts = new List<string>();
                    int total = singles + jumps + hands + quads;
                    if (total > 0) {
                        if (jumps + hands + quads > singles) parts.Add("jumpstream"); else parts.Add("singles");
                        if (hands + quads > total * 0.15) parts.Add("chord-heavy");
                    }
                    if (jacks >= 2) parts.Add("jacks");
                    if (bursts > 0) parts.Add("bursts");
                    return parts.Count > 0 ? string.Join(",", parts) : "mixed";
                }

                private static string ComputePatternProfile(Qua map)
                {
                    var notes = map.HitObjects.OrderBy(x => x.StartTime).ToList();
                    if (notes.Count == 0) return "empty";
                    int singles=0, jumps=0, hands=0, quads=0;
                    
                    foreach (var g in notes.GroupBy(x => x.StartTime)) {
                        var c = g.Count();
                        if (c <= 1) singles++; else if (c == 2) jumps++; else if (c == 3) hands++; else quads++;
                    }
                    int total = Math.Max(1, singles + jumps + hands + quads);
                    
                    var inv = CultureInfo.InvariantCulture;
                    string r(int v) => ((double)v / total).ToString("F2", inv);
                    int keys = map.Mode == GameMode.Keys7 ? 7 : 4;
                    double mid = (keys - 1) / 2.0;
                    int leftN = notes.Count(x => x.Lane - 1 < mid);
                    int rightN = notes.Count(x => x.Lane - 1 > mid);
                    double bias = (rightN - leftN) / (double)Math.Max(1, leftN + rightN);
                    string hand = bias > 0.10 ? "RH-heavy" : bias < -0.10 ? "LH-heavy" : "Balanced";

                    return $"ChordDensity{{S:{r(singles)},J:{r(jumps)},H:{r(hands)},Q:{r(quads)}}} Hand:{hand}"; 
                }
            }
        }
        '@
        Set-Content -Path $path3 -Value $rhCode -Encoding UTF8

        # ========================================================
        # 4. GameplayScreen.cs (Inject Export Call cleanly)
        # ========================================================
        $path4 = "Quaver.Shared/Screens/Gameplay/GameplayScreen.cs"
        Write-Host "Force Writing: $path4"
        if (Test-Path "BuildPatches/GameplayScreen.cs") { $path4 = "BuildPatches/GameplayScreen.cs" }
        
        $gpContent = Get-Content $path4 -Raw
        $oldExport = 'ReplayHelper.ExportReplayToText(ReplayCapturer.Replay, "last_replay_debug.txt");'
        $newExport = 'if (Ruleset?.ScoreProcessor?.Stats != null && Map != null) { ReplayHelper.ExportDetailedAnalysis(Ruleset.ScoreProcessor.Stats, Map, Ruleset.ScoreProcessor); }'
        
        if ($gpContent.Contains($oldExport)) {
            $gpContent = $gpContent.Replace($oldExport, $newExport)
        } else {
            $gpContent = $gpContent.Replace('Metronome?.Dispose();', "$newExport `n            Metronome?.Dispose();")
        }
        Set-Content -Path $path4 -Value $gpContent -Encoding UTF8
        Set-Content -Path "Quaver.Shared/Screens/Gameplay/GameplayScreen.cs" -Value $gpContent -Encoding UTF8

        # ========================================================
        # 5. SelectionScreen.cs (RESTORE SMART REFRESH)
        # ========================================================
        $path5 = "Quaver.Shared/Screens/Selection/SelectionScreen.cs"
        Write-Host "Force Writing: $path5"
        
        $ssCode = @'
        using System;
        using System.Collections.Generic;
        using System.Linq;
        using Microsoft.Xna.Framework;
        using Microsoft.Xna.Framework.Input;
        using Quaver.API.Enums;
        using Quaver.API.Helpers;
        using Quaver.Server.Client.Enums;
        using Quaver.Server.Client.Objects;
        using Quaver.Shared.Audio;
        using Quaver.Shared.Config;
        using Quaver.Shared.Database.Maps;
        using Quaver.Shared.Database.Playlists;
        using Quaver.Shared.Database.Scores;
        using Quaver.Shared.Database.Settings;
        using Quaver.Shared.Discord;
        using Quaver.Shared.Graphics.Notifications;
        using Quaver.Shared.Graphics.Transitions;
        using Quaver.Shared.Modifiers;
        using Quaver.Shared.Modifiers.Mods;
        using Quaver.Shared.Online;
        using Quaver.Shared.Scheduling;
        using Quaver.Shared.Screens.Edit;
        using Quaver.Shared.Screens.Importing;
        using Quaver.Shared.Screens.Loading;
        using Quaver.Shared.Screens.Main;
        using Quaver.Shared.Screens.Multi;
        using Quaver.Shared.Screens.Selection.UI;
        using Quaver.Shared.Screens.Selection.UI.Dialogs;
        using Quaver.Shared.Screens.Selection.UI.FilterPanel.Search;
        using Quaver.Shared.Screens.Selection.UI.Leaderboard;
        using Quaver.Shared.Screens.Selection.UI.Maps;
        using Quaver.Shared.Screens.Selection.UI.Mapsets;
        using Quaver.Shared.Screens.Tournament;
        using Quaver.Shared.Skinning;
        using Wobble;
        using Wobble.Audio.Tracks;
        using Wobble.Bindables;
        using Wobble.Graphics.UI.Dialogs;
        using Wobble.Input;
        using Wobble.Logging;

        namespace Quaver.Shared.Screens.Selection
        {
            public sealed class SelectionScreen : QuaverScreen, IHasLeftPanel
            {
                public override QuaverScreenType Type { get; } = QuaverScreenType.Select;
                public bool IsMultiplayer { get; }
                public Bindable<List<Mapset>> AvailableMapsets { get; private set; }
                public Bindable<string> CurrentSearchQuery { get; private set; }
                public Bindable<SelectContainerPanel> ActiveLeftPanel { get; set; }
                public Bindable<SelectScrollContainerType> ActiveScrollContainer { get; private set; }
                private Random Rng { get; } = new Random();
                public Stack<Map> RngHistory { get; set; } = new Stack<Map>();
                public static event EventHandler<RandomMapsetSelectedEventArgs> RandomMapsetSelected;
                private bool IsExportingMapset { get; set; }
                public Bindable<bool> IsPlayTestingInPreview { get; private set; }

                public SelectionScreen()
                {
                    IsMultiplayer = OnlineManager.CurrentGame != null;
                    if (MapsetImporter.Queue.Count > 0 || QuaverSettingsDatabaseCache.OutdatedMaps.Count != 0 || MapDatabaseCache.MapsToUpdate.Count != 0)
                    {
                        Exit(() => new ImportingScreen(null, true));
                        return;
                    }
                    if (IsMultiplayer) OnlineManager.Client?.SetGameCurrentlySelectingMap(true);
                    else SetRichPresence();
                    InitializeSearchQueryBindable();
                    InitializeAvailableMapsetsBindable();
                    InitializeActiveLeftPanelBindable();
                    InitializeActiveScrollContainerBindable();
                    InitializeTestPlayingBindable();
                    InitializeSelectedPlaylist();
                    AvailableMapsets.Value = MapsetHelper.FilterMapsets(CurrentSearchQuery);
                    MapManager.MapsetDeleted += OnMapsetDeleted;
                    MapManager.MapDeleted += OnMapDeleted;
                    MapManager.MapUpdated += OnMapUpdated;
                    MapManager.SongRequestPlayed += OnSongRequestPlayed;
                    ConfigManager.AutoLoadOsuBeatmaps.ValueChanged += OnAutoLoadOsuBeatmapsChanged;
                    View = new SelectionScreenView(this);
                }

                public override void OnFirstUpdate() { GameBase.Game.GlobalUserInterface.Cursor.Alpha = 1; FadeAudioTrackIn(); SkinManager.StartWatching(); ScreenExiting += (_, _) => SkinManager.StopWatching(); base.OnFirstUpdate(); }
                public override void Update(GameTime gameTime) { if (!Exiting) GameBase.Game.GlobalUserInterface.Cursor.Alpha = 1; HandleInput(gameTime); base.Update(gameTime); }
                public override void Destroy() { CurrentSearchQuery?.Dispose(); AvailableMapsets?.Dispose(); ActiveLeftPanel?.Dispose(); ActiveScrollContainer?.Dispose(); IsPlayTestingInPreview?.Dispose(); RandomMapsetSelected = null; MapManager.MapsetDeleted -= OnMapsetDeleted; MapManager.MapDeleted -= OnMapDeleted; MapManager.MapUpdated -= OnMapUpdated; MapManager.SongRequestPlayed -= OnSongRequestPlayed; SkinManager.StopWatching(); ConfigManager.AutoLoadOsuBeatmaps.ValueChanged -= OnAutoLoadOsuBeatmapsChanged; base.Destroy(); }

                private void InitializeSearchQueryBindable() => CurrentSearchQuery = new Bindable<string>(null) { Value = FilterPanelSearchBox.PreviousSearchTerm };
                private void InitializeAvailableMapsetsBindable() => AvailableMapsets = new Bindable<List<Mapset>>(null) { Value = new List<Mapset>() };
                private void InitializeActiveLeftPanelBindable() { ActiveLeftPanel = new Bindable<SelectContainerPanel>(SelectContainerPanel.Leaderboard) { Value = SelectContainerPanel.Leaderboard }; }
                private void InitializeActiveScrollContainerBindable() { ActiveScrollContainer = new Bindable<SelectScrollContainerType>(SelectScrollContainerType.Mapsets) { Value = SelectScrollContainerType.Mapsets }; if (ConfigManager.SelectGroupMapsetsBy.Value == GroupMapsetsBy.Playlists) ActiveScrollContainer.Value = SelectScrollContainerType.Playlists; if (PlaylistManager.Selected.Value != null && ConfigManager.SelectGroupMapsetsBy.Value == GroupMapsetsBy.Playlists && PlaylistManager.Selected.Value.Maps.Count > 0) ActiveScrollContainer.Value = SelectScrollContainerType.Mapsets; }
                private void InitializeTestPlayingBindable() => IsPlayTestingInPreview = new Bindable<bool>(false) { Value = false };
                private void InitializeSelectedPlaylist() { if (PlaylistManager.Selected.Value == null && PlaylistManager.Playlists.Count != 0) PlaylistManager.Selected.Value = PlaylistManager.Playlists.First(); }

                private void HandleInput(GameTime gameTime)
                {
                    if (Exiting || DialogManager.Dialogs.Count != 0) return;
                    HandleKeyPressEscape(); HandleKeyPressF1(); HandleKeyPressF2(); HandleKeyPressF3(); HandleKeyPressF4(); HandleKeyPressF5(); HandleKeyPressEnter(); HandleKeyPressControlInput(); HandleThumb1MouseButtonClick();
                    if (ActiveLeftPanel.Value == SelectContainerPanel.Leaderboard) HandleKeyPressTab();
                }

                private void HandleKeyPressEscape() { if (!KeyboardManager.IsUniqueKeyPress(Keys.Escape)) return; HandleBackAction(); }
                private void HandleKeyPressF1() { if (!KeyboardManager.IsUniqueKeyPress(Keys.F1)) return; if (ActiveLeftPanel.Value == SelectContainerPanel.Modifiers) ActiveLeftPanel.Value = SelectContainerPanel.Leaderboard; else ActiveLeftPanel.Value = SelectContainerPanel.Modifiers; }
                private void HandleKeyPressF2() { if (!KeyboardManager.IsUniqueKeyPress(Keys.F2)) return; if (KeyboardManager.IsShiftDown()) SelectPrevRandomMap(); else SelectRandomMap(); }
                private void HandleKeyPressF3() { if (KeyboardManager.IsCtrlDown()) return; if (!KeyboardManager.IsUniqueKeyPress(Keys.F3)) return; if (ActiveLeftPanel.Value == SelectContainerPanel.MapPreview) ActiveLeftPanel.Value = SelectContainerPanel.Leaderboard; else ActiveLeftPanel.Value = SelectContainerPanel.MapPreview; }
                private void HandleKeyPressF4() { if (KeyboardManager.IsCtrlDown()) return; if (!KeyboardManager.IsUniqueKeyPress(Keys.F4)) return; if (ActiveLeftPanel.Value == SelectContainerPanel.UserProfile) ActiveLeftPanel.Value = SelectContainerPanel.Leaderboard; else ActiveLeftPanel.Value = SelectContainerPanel.UserProfile; }
                
                private void HandleKeyPressF5() 
                {
                    if (!KeyboardManager.IsUniqueKeyPress(Keys.F5)) return;
                    if (KeyboardManager.IsCtrlDown()) { DialogManager.Show(new RefreshDialog()); return; }

                    NotificationManager.Show(NotificationLevel.Info, "Scanning for new maps...");
                    ThreadScheduler.Run(() =>
                    {
                        var found = MapManager.DetectNewMapsets();
                        if (found) {
                            if (MapsetImporter.Queue.Count > 0) { Exit(() => new ImportingScreen(null, true)); return; }
                            NotificationManager.Show(NotificationLevel.Success, "New maps loaded!");
                            lock (AvailableMapsets.Value) AvailableMapsets.Value = MapsetHelper.FilterMapsets(CurrentSearchQuery);
                        } else { NotificationManager.Show(NotificationLevel.Success, "No new maps found."); }
                    });
                }

                private void HandleKeyPressEnter() { if (!KeyboardManager.IsUniqueKeyPress(Keys.Enter) || KeyboardManager.IsAltDown()) return; switch (ActiveScrollContainer.Value) { case SelectScrollContainerType.Mapsets: if (MapsetHelper.IsSingleDifficultySorted()) ExitToGameplay(); else ActiveScrollContainer.Value = SelectScrollContainerType.Maps; break; case SelectScrollContainerType.Maps: ExitToGameplay(); break; case SelectScrollContainerType.Playlists: ActiveScrollContainer.Value = SelectScrollContainerType.Mapsets; break; default: throw new ArgumentOutOfRangeException(); } }
                public static void HandleKeyPressTab() { if (!KeyboardManager.IsUniqueKeyPress(Keys.Tab)) return; var index = (int)ConfigManager.LeaderboardSection.Value; var length = Enum.GetNames(typeof(LeaderboardType)).Length; int newIndex; if (KeyboardManager.IsShiftDown()) { if (index - 1 >= 0) newIndex = index - 1; else newIndex = length - 1; } else { if (index + 1 < length) newIndex = index + 1; else newIndex = 0; } ConfigManager.LeaderboardSection.Value = (LeaderboardType)newIndex; }
                private void HandleKeyPressControlInput() { if (!KeyboardManager.IsCtrlDown()) return; var shiftHeld = KeyboardManager.IsShiftDown(); if (KeyboardManager.IsUniqueKeyPress(ConfigManager.KeyIncreaseGameplayAudioRate.Value)) ModManager.AddSpeedMods(GetNextRate(true, shiftHeld)); if (KeyboardManager.IsUniqueKeyPress(ConfigManager.KeyDecreaseGameplayAudioRate.Value)) ModManager.AddSpeedMods(GetNextRate(false, shiftHeld)); if (KeyboardManager.IsUniqueKeyPress(Keys.D0)) ConfigManager.Pitched.Value = !ConfigManager.Pitched.Value; if (KeyboardManager.IsUniqueKeyPress(ConfigManager.KeyToggleMirror.Value)) { if (ModManager.IsActivated(ModIdentifier.Mirror)) ModManager.RemoveMod(ModIdentifier.Mirror); else ModManager.AddMod(ModIdentifier.Mirror); } ChangeScrollSpeed(); }
                private void HandleThumb1MouseButtonClick() { if (!MouseManager.IsUniqueClick(MouseButton.Thumb1)) return; var view = (SelectionScreenView)View; switch (ActiveScrollContainer.Value) { case SelectScrollContainerType.Mapsets: if (ConfigManager.SelectGroupMapsetsBy.Value != GroupMapsetsBy.Playlists) return; if (!view.MapsetContainer.IsHovered()) return; ActiveScrollContainer.Value = SelectScrollContainerType.Playlists; break; case SelectScrollContainerType.Maps: if (!view.MapContainer.IsHovered()) return; ActiveScrollContainer.Value = SelectScrollContainerType.Mapsets; break; case SelectScrollContainerType.Playlists: return; } }
                public void HandleBackAction() { switch (ActiveLeftPanel.Value) { case SelectContainerPanel.Leaderboard: case SelectContainerPanel.MapPreview: if (ActiveScrollContainer.Value == SelectScrollContainerType.Maps) { ActiveScrollContainer.Value = SelectScrollContainerType.Mapsets; return; } if (ActiveScrollContainer.Value == SelectScrollContainerType.Mapsets && ConfigManager.SelectGroupMapsetsBy.Value == GroupMapsetsBy.Playlists) { ActiveScrollContainer.Value = SelectScrollContainerType.Playlists; return; } if (ActiveLeftPanel.Value == SelectContainerPanel.Leaderboard) ExitToMenu(); if (ActiveLeftPanel.Value == SelectContainerPanel.MapPreview) ActiveLeftPanel.Value = SelectContainerPanel.Leaderboard; break; default: ActiveLeftPanel.Value = SelectContainerPanel.Leaderboard; break; } }
                public static float GetNextRate(bool faster, bool forceHalfRate = false) { var current = ModHelper.GetRateFromMods(ModManager.Mods); var adjustment = 0.1f; if (current < 1.0f || forceHalfRate || current == 1.0f && !faster) adjustment = 0.05f; var next = current + adjustment * (faster ? 1f : -1f); next = Math.Clamp(next, ModSpeed.MinSpeed, ModSpeed.MaxSpeed); return (float)Math.Round(next, 2); }
                private void FadeAudioTrackIn() { if (ConfigManager.VolumeMusic == null) return; if (AudioEngine.Track != null && AudioEngine.Track.IsPlaying) AudioEngine.Track?.Fade(100, 300); }
                private void ChangeScrollSpeed() { if (MapManager.Selected.Value == null) return; var scrollSpeed = ConfigManager.ScrollSpeeds[MapManager.Selected.Value.Mode]; var changed = false; var speedIncrease = KeyboardManager.IsShiftDown() ? 1 : 10; if (KeyboardManager.IsUniqueKeyPress(Keys.F3)) { scrollSpeed.Value -= speedIncrease; changed = true; } else if (KeyboardManager.IsUniqueKeyPress(Keys.F4)) { scrollSpeed.Value += speedIncrease; changed = true; } if (changed) NotificationManager.Show(NotificationLevel.Info, $"Your {ModeHelper.ToShortHand(MapManager.Selected.Value.Mode)} scroll speed has been changed to: {scrollSpeed.Value / 10f:0.0}"); }
                public void SelectPrevRandomMap() { if (AvailableMapsets.Value.Count == 0) return; if (RngHistory.Count == 0) return; var map = RngHistory.Pop(); var index = AvailableMapsets.Value.FindIndex(0, AvailableMapsets.Value.Count, x => x.Maps.Contains(map)); while (index == -1) { if (RngHistory.Count == 0) return; map = RngHistory.Pop(); index = AvailableMapsets.Value.FindIndex(0, AvailableMapsets.Value.Count, x => x.Maps.Contains(map)); } MapManager.Selected.Value = map; RandomMapsetSelected?.Invoke(this, new RandomMapsetSelectedEventArgs(map.Mapset, index)); }
                public void SelectRandomMap() { if (AvailableMapsets.Value.Count == 0) return; ActiveScrollContainer.Value = SelectScrollContainerType.Mapsets; var index = Rng.Next(AvailableMapsets.Value.Count); var mapIndex = Rng.Next(AvailableMapsets.Value[index].Maps.Count); RngHistory.Push(MapManager.Selected.Value); MapManager.Selected.Value = AvailableMapsets.Value[index].Maps[mapIndex]; RandomMapsetSelected?.Invoke(this, new RandomMapsetSelectedEventArgs(AvailableMapsets.Value[index], index)); }
                public void ExitToGameplay() { if (MapManager.Selected.Value == null) return; if (OnlineManager.CurrentGame != null) { SelectMultiplayerMap(); return; } if (SkinManager.Skin.SoundSelect != null) { AudioEngine.Track.Pause(); SkinManager.Skin.SoundSelect.CreateChannel().Play(); } if (OnlineManager.IsSpectatingSomeone) OnlineManager.Client?.StopSpectating(); if (ModManager.IsActivated(ModIdentifier.Coop)) { Exit(() => new TournamentScreen(2)); return; } Exit(() => new MapLoadingScreen(new List<Score>())); }
                public void ExitToMenu() { Exit(() => { if (IsMultiplayer) { OnlineManager.Client?.SetGameCurrentlySelectingMap(false); return new MultiplayerGameScreen(); } return new MainMenuScreen(); }); }
                public void ExitToEditor() { if (MapManager.Selected.Value == null) return; if (AudioEngine.Track != null && !AudioEngine.Track.IsStopped) AudioEngine.Track.Stop(); if (OnlineManager.CurrentGame != null) { NotificationManager.Show(NotificationLevel.Error, "You cannot use the editor while playing multiplayer."); return; } if (OnlineManager.IsSpectatingSomeone) OnlineManager.Client?.StopSpectating(); try { IAudioTrack track; try { track = new AudioTrack(MapManager.GetAudioPath(MapManager.Selected.Value), false, false); } catch (Exception) { track = new AudioTrackVirtual(MapManager.Selected.Value.SongLength + 5000); } ModManager.RemoveAllMods(); Exit(() => new EditScreen(MapManager.Selected.Value, track)); } catch (Exception e) { Logger.Error(e, LogType.Runtime); NotificationManager.Show(NotificationLevel.Error, "Failed to load the editor with this map!"); } }
                private void SelectMultiplayerMap() { var map = MapManager.Selected.Value; if (!CheckMultiplayerDifficultyRange()) return; if (!CheckMultiplayerSongLength()) return; if (!CheckMultiplayerGameMode()) return; if (!CheckMultiplayerLongNotePercentage()) return; Transitioner.FadeIn(); ThreadScheduler.Run(() => { OnlineManager.Client.ChangeMultiplayerGameMap(map.Md5Checksum, map.MapId, map.MapSetId, map.ToString(), (byte)map.Mode, map.DifficultyFromMods(ModManager.Mods), map.GetDifficultyRatings(), map.GetJudgementCount(), MapManager.Selected.Value.GetAlternativeMd5()); OnlineManager.Client.SetGameCurrentlySelectingMap(false); Exit(() => new MultiplayerGameScreen()); }); }
                private bool CheckMultiplayerDifficultyRange() { var diff = MapManager.Selected.Value.DifficultyFromMods(ModManager.Mods); if (diff < OnlineManager.CurrentGame.MinimumDifficultyRating || diff > OnlineManager.CurrentGame.MaximumDifficultyRating) { NotificationManager.Show(NotificationLevel.Error, $"Difficulty rating must be between {OnlineManager.CurrentGame.MinimumDifficultyRating} and {OnlineManager.CurrentGame.MaximumDifficultyRating} for this multiplayer match!"); return false; } return true; }
                private bool CheckMultiplayerSongLength() { var length = MapManager.Selected.Value.SongLength * ModHelper.GetRateFromMods(ModManager.Mods) / 1000; if (length > OnlineManager.CurrentGame.MaximumSongLength) { NotificationManager.Show(NotificationLevel.Error, $"The maximum length allowed for this multiplayer match is: {OnlineManager.CurrentGame.MaximumSongLength} seconds"); return false; } return true; }
                private bool CheckMultiplayerGameMode() { if (!OnlineManager.CurrentGame.AllowedGameModes.Contains((byte)MapManager.Selected.Value.Mode)) { NotificationManager.Show(NotificationLevel.Error, "You cannot pick maps of this game mode in this multiplayer match!"); return false; } return true; }
                private bool CheckMultiplayerLongNotePercentage() { var map = MapManager.Selected.Value; if (map.LNPercentage < OnlineManager.CurrentGame.MinimumLongNotePercentage || map.LNPercentage > OnlineManager.CurrentGame.MaximumLongNotePercentage) { NotificationManager.Show(NotificationLevel.Error, $"You cannot select this map. The long note percentage must be between {OnlineManager.CurrentGame.MinimumLongNotePercentage}%-{OnlineManager.CurrentGame.MaximumLongNotePercentage}% for this multiplayer match."); return false; } return true; }
                public void ExportSelectedMapset() { if (MapManager.Selected.Value == null) return; if (IsExportingMapset) { NotificationManager.Show(NotificationLevel.Warning, "Slow down! You must wait for your previous mapset to export"); return; } IsExportingMapset = true; ThreadScheduler.Run(() => { NotificationManager.Show(NotificationLevel.Info, "Exporting mapset to zip archive. Please wait!"); MapManager.Selected.Value.Mapset.ExportToZip(); IsExportingMapset = false; NotificationManager.Show(NotificationLevel.Success, $"Successfully exported {MapManager.Selected.Value.Mapset.Artist} - {MapManager.Selected.Value.Mapset.Title}!"); }); }
                private void SetRichPresence() { DiscordHelper.Presence.PartySize = 0; DiscordHelper.Presence.PartyMax = 0; DiscordHelper.Presence.StartTimestamp = 0; DiscordHelper.Presence.EndTimestamp = 0; DiscordHelper.Presence.LargeImageText = OnlineManager.GetRichPresenceLargeKeyText(ConfigManager.SelectedGameMode.Value); DiscordHelper.Presence.SmallImageKey = ModeHelper.ToShortHand(ConfigManager.SelectedGameMode.Value).ToLower(); DiscordHelper.Presence.SmallImageText = ModeHelper.ToLongHand(ConfigManager.SelectedGameMode.Value); Helpers.RichPresenceHelper.UpdateRichPresence("In the menus", "Selecting a song"); }
                private void OnMapsetDeleted(object sender, MapsetDeletedEventArgs e) { ThreadScheduler.Run(() => { var index = 0; if (e.Index == -1) index = 0; if (e.Index - 1 >= 0) index = e.Index - 1; lock (AvailableMapsets.Value) AvailableMapsets.Value = MapsetHelper.FilterMapsets(CurrentSearchQuery); if (index >= 0 && index < AvailableMapsets.Value.Count) { MapManager.SelectMapFromMapset(AvailableMapsets.Value[index]); return; } lock (AudioEngine.Track) { if (AudioEngine.Track.IsDisposed && !AudioEngine.Track.IsStopped) AudioEngine.Track.Dispose(); } }); }
                private void OnMapDeleted(object sender, MapDeletedEventArgs e) { ThreadScheduler.Run(() => { lock (AvailableMapsets.Value) AvailableMapsets.Value = MapsetHelper.FilterMapsets(CurrentSearchQuery); var mapsetIndex = AvailableMapsets.Value.FindIndex(x => x.Maps.Contains(e.Map)); if (mapsetIndex == -1 && AvailableMapsets.Value.Count != 0) { MapManager.SelectMapFromMapset(AvailableMapsets.Value.First()); return; } lock (AudioEngine.Track) { if (AudioEngine.Track.IsDisposed && !AudioEngine.Track.IsStopped) AudioEngine.Track.Dispose(); } }); }
                private void OnMapUpdated(object sender, MapUpdatedEventArgs e) => AvailableMapsets.Value = MapsetHelper.FilterMapsets(CurrentSearchQuery);
                private void OnSongRequestPlayed(object sender, SongRequestPlayedEventArgs e) { ThreadScheduler.Run(() => { MapManager.Selected.Value = e.Map; lock (AvailableMapsets.Value) AvailableMapsets.Value = MapsetHelper.FilterMapsets(CurrentSearchQuery); }); }
                private void OnAutoLoadOsuBeatmapsChanged(object sender, BindableValueChangedEventArgs<bool> e) => Exit(() => new ImportingScreen(null, true));
                public override UserClientStatus GetClientStatus() => new UserClientStatus(ClientStatus.Selecting, -1, "", 0, "", 0);
            }
        }
        '@
        Set-Content -Path $path5 -Value $ssCode -Encoding UTF8

    - name: Setup .NET
      uses: actions/setup-dotnet@v3
      with:
        dotnet-version: 6.0.x

    - name: Fix Dependencies
      shell: pwsh
      run: |
        $proj = "Quaver.Shared/Quaver.Shared.csproj"
        if (Test-Path $proj) {
            $content = Get-Content $proj -Raw
            $content = $content -replace '<ProjectReference Include=".*Quaver\.Server\.Client\.csproj" />', '<Reference Include="Quaver.Server.Client"><HintPath>Quaver.Server.Client.dll</HintPath></Reference>'
            Set-Content $proj $content
        }
        dotnet add Quaver.Shared/Quaver.Shared.csproj package TimeAgo

    - name: Restore and Build
      run: |
        dotnet clean Quaver/Quaver.csproj
        dotnet restore Quaver/Quaver.csproj
        dotnet publish Quaver/Quaver.csproj -c Release -r win-x64 --no-self-contained -o ./QuaverMod

    - name: Upload
      uses: actions/upload-artifact@v4
      with:
        name: Quaver-Smart-Refresh-Mod
        path: ./QuaverMod
